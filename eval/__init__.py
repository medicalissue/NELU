"""Robustness evaluation entry points.

Two scripts, one per domain:

* :mod:`eval.imagenet_robustness` — ImageNet-C (corruption),
  ImageNet-A (natural adversarial), ImageNet-R (renditions), and
  ImageNet-O (OOD) accuracy for a trained ImageNet-1k checkpoint.
* :mod:`eval.cifar_robustness` — CIFAR-100-C corruption accuracy for a
  trained CIFAR-100 checkpoint.
"""
